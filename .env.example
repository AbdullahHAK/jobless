DATABASE_URL=postgresql://jobless:jobless@localhost:5432/jobless

# Only needed to run src/jobless/digest.py (email digests) locally - the
# scraper and API work fine without these.
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev
API_BASE_URL=http://localhost:8000
